data:
  raw_dir: "data/raw"
  model_dir: "models/current"
  report_dir: "data/demo"

privacy:
  hash_salt: "demo_salt_change_me"

detection:
  business_hours:
    start: 8
    end: 18
  contamination: 0.01
  ae_weight: 0.6
  svm_weight: 0.4
  context_weight: 0.2

canary:
  tokens:
    - "DEMF_CANARY_TOKEN"
    - "canary.example.com"

modeling:
  random_state: 42
  max_iter: 300
  train_split_ratio: 0.80

evaluation:
  labels_path: data/labels.csv
